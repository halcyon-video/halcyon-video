# 3D Model Attribution

The six `checkout-counter-*.glb` variants are original Halcyon millwork, covered
by the repository's license. Editable Blender source and the reproducible
mesh-authoring script live in `tools/models/checkout-counter.blend` and
`tools/models/checkout-counter.py`. They contain no downloaded geometry,
textures, or chain trademarks. See `docs/checkout-counter-model.md` for the
construction and placement contract.

Downloaded from [Poly Pizza](https://poly.pizza) and [Sketchfab](https://sketchfab.com),
used for the store's prop dressing.

| File | Model | Author | License |
|---|---|---|---|
| `retro_tv.glb` | [Retro TV](https://poly.pizza/m/2tfjTOK1Lh4) | Alex Safayan | [CC-BY 3.0](https://creativecommons.org/licenses/by/3.0/) |
| `car_sedan.glb` | [Car](https://poly.pizza/m/Cz6yDaUcM9) | Quaternius | CC0 |
| `car_sports.glb` | [Sports Car](https://poly.pizza/m/1mkmFkAz5v) | Quaternius | CC0 |

| `car_hatchback.glb` | [Car Hatchback](https://poly.pizza/m/BG0KAhmGDt) | Kay Lousberg | CC0 |

| `coffee_table.glb` | Original Halcyon oak/glass coffee table (#215) | Halcyon contributors | Repository license; embedded ambientCG WoodFloor043 maps CC0 |
| `tv_ceiling.glb` / `tv_hero.glb` | [CRT-TV](https://sketchfab.com/3d-models/crt-tv-bc61b13e2ac2472dbfcdd1dd2fa8c584) | pawelk1568890 | [CC-BY 4.0](https://creativecommons.org/licenses/by/4.0/) |
| `balloon.glb` | Original generic latex balloon, neck, knot and continuous cotton tether; scripted geometry and procedural surface maps | Halcyon contributors | Repository license; [source and contract](../../docs/balloon-model.md) |

The `vcr.glb` and `dvd_player.glb` decks are original Halcyon models, covered by
this repository's license. They replace previously unfilled download slots;
no geometry or textures from those proposed downloads were used. Editable
collections and the reproducible authoring script are in
`tools/models/av-decks.blend` and `tools/models/av-decks.py`. See
`docs/av-deck-models.md` for dimensions, construction, resource costs and the
live display contract.

The originally vetted free TVs — Zgon's "Old Television from 90's" and
LiuMeowMeow's "TV Sony Trinitron", both CC-BY 4.0 — remain vetted alternates
if the user-supplied TV ever needs replacing.

The `rental-terminal.glb` and `rental-keyboard.glb` register models are
original Halcyon Blender meshes under this repository's license, authored by
`tools/models/counter-terminal.py` (editable `tools/models/counter-terminal.blend`).
They replaced the downloaded `crt_monitor.glb` ("CRT Monitor" by Jarlan Perez,
CC-BY 3.0, poly.pizza/m/8jVB0zIXKCv) and `keyboard.glb` ("Computer Keyboard" by
Kenney, CC0): the monitor's overall proportions were measured from that CC-BY
model so the approved silhouette carried over, but no geometry, UVs or imagery
from either download is in the new files, and their embedded occlusion and
grain maps are generated. See `docs/counter-terminal-model.md`.

The `ceiling-television.glb` and `screening-television.glb` models are original
Halcyon Blender meshes under this repository's license (an earlier
`rental-terminal.glb` from the same script was superseded by the register
models above). Editable source and the reproducible script are
`tools/models/store-crts.blend` and `tools/models/store-crts.py`. Their embedded roughness map is generated
original material grain, not downloaded imagery. See
`docs/store-remodel-models.md` for dimensions, materials and integration.

The expanded `shelf-components.glb` and corresponding Blender source/script are
original Halcyon construction under this repository's license, including the
laminate carcass profiles, end panels, steel standards and formed feet.

The `genre-endcap-tapered.glb`, `genre-endcap-straight.glb`,
`four-sided-merchandiser.glb` and `bargain-tub.glb` fixtures are original
Halcyon Blender meshes under this repository's license. Each has an editable
same-named `.blend` in `tools/models/`; `tools/models/display-fixtures.py`
reproduces all four. No downloaded geometry, photographs, embedded textures
or chain marks are included. See `docs/display-fixture-models.md` for the
construction, stock anchors, named finish roles and measured resource costs.

The `customer-pole-display.glb` customer VFD is an original generic Halcyon
model under this repository's license. Named editable parts and the scripted
mesh authoring source are `tools/models/customer-pole-display.blend` and
`tools/models/customer-pole-display.py`. It contains no downloaded geometry,
images or manufacturer marks. See `docs/customer-pole-display-model.md` for
its dimensions, material roles, resource costs and stable message/anchor contract.

The `tape-rewinder.glb` countertop shell is original generic Halcyon construction
under this repository's license. Editable named parts and reproducible mesh
authoring live in `tools/models/tape-rewinder.blend` and
`tools/models/tape-rewinder.py`. No downloaded meshes, images or manufacturer
marks are included. See `docs/tape-rewinder-model.md` for its dimensions,
resource costs and preserved live reel, logo and placement contracts.

The `recessed-downlight.glb` commercial downlight luminaire is original generic
Halcyon construction under this repository's license. Editable named parts and
reproducible mesh authoring live in `tools/models/recessed-downlight.blend` and
`tools/models/recessed-downlight.py`. No downloaded meshes, images or manufacturer
marks are included. See `docs/recessed-downlight-model.md` for its dimensions,
material roles, resource costs and ceiling soffit integration.

## Customer information terminal surround

`customer-information-terminal.glb` is original scripted Blender geometry for
#280: a generic, unplaced construction study with no embedded reference images
or branded print. Editable source, generator, uncertainty and attachment contract:
`tools/models/customer-information-terminal.blend`,
`tools/models/customer-information-terminal.py`, and
`docs/customer-information-terminal-model.md`. The source photograph was inspected
privately; no faithful skin is distributed. The runtime display is an explicitly
generic envelope pending device and placement references.

`car_hatchback.glb` is original generic Halcyon scripted Blender construction,
covered by this repository's license. Editable parts and authoring script:
`tools/models/car-hatchback.blend` and `tools/models/car-hatchback.py`.
It replaces Kay Lousberg's CC0 [Car Hatchback](https://poly.pizza/m/BG0KAhmGDt).
No downloaded geometry, textures or badges are carried over. An original 1987
Volkswagen brochure photograph was inspected for period construction only;
this is not a branded replica. See `docs/car-hatchback-model.md` for reference
links, dimensions, material roles, placement and full-lot resource costs.

<<<<<<< HEAD
### Concrete sidewalk, curb and gutter kit (#265)

`curb-kit.glb` is original generic Halcyon geometry, authored by scripted Blender
mesh construction from existing scene dimensions; no external models, branded
references or texture images. Editable source and generator are
`tools/models/curb-kit.blend` and `tools/models/curb-kit.py`. Runtime reuses the
existing procedural concrete texture. See `docs/curb-kit-model.md` for dimensions,
material roles, fitting and validation.

### Exterior tape-return kiosk

`exterior-return-kiosk.glb` is original generic Blender mesh work for Halcyon
(#262), under the repository license. Editable source and generator are in
`tools/models/exterior-return-kiosk.*`; dimensions/provenance are documented in
`docs/exterior-return-kiosk-model.md`. No third-party textures or geometry, chain
branding, or claimed reference-exact QuikDrop reconstruction.

## Entrance bollard (`entrance-bollard.glb`)

Original generic scripted Blender mesh for #263; no external geometry or textures. Editable source and generator: `tools/models/entrance-bollard.blend` and `tools/models/entrance-bollard.py`. Dimensions, material roles, provenance and installation: [`docs/entrance-bollard-model.md`](../../docs/entrance-bollard-model.md). Not a measured period replica.


## Parking-lot light pole and shoebox luminaire (#264)

- `parking-lamp.glb`: original generic fixture, scripted mesh authoring in Blender 5.2; no downloaded geometry, textures, branded art, or manufacturer likeness.
- Editable source: `tools/models/parking-lamp.blend`; reproducible generator: `tools/models/parking-lamp.py`.
- Dimensions are design estimates fitted to the existing 13-foot lamp anchors, not measured period hardware.
- Geometry and delivery contract: `docs/parking-lamp-model.md`. Distributed under the repository's license.

=======
## Parked sports coupe

`car_sports.glb` is now original Halcyon scripted Blender geometry under this
repository's license. Editable named parts, UVs and material roles are in
`tools/models/sports-coupe.blend`; `tools/models/sports-coupe.py` reproduces the
source and optimized export. No downloaded geometry, images or manufacturer
marks are included. The former CC0 Quaternius
[Sports Car](https://poly.pizza/m/1mkmFkAz5v) was inspected for the parking anchor
and street-facing orientation, and replaced in full. Nissan's
[1990 180SX heritage photograph](https://www.nissan-global.com/EN/HERITAGE_COLLECTION/nissan_180SX_TYPE_II.html)
was inspected as a period design study only. See
`docs/sports-coupe-model.md` for dimensions, provenance limits, geometry costs
and the preserved normalization, finish, fallback and lifecycle contracts.
>>>>>>> f438edb (fix(halcyon): resolve #268 - Blender: replace the lot’s period sports coupe with an editable model)

## Projection reel (#245)

projection-reel.glb, projection-reel-wound.glb and the six alpha poses under
projection-reel/ are original generic scripted Blender artwork for Halcyon.
No downloaded mesh or source photograph is incorporated; no period-replica
claim is made. Editable source and generators: tools/models/projection-reel*.
See docs/projection-reel-model.md for assumptions, UVs, roles and consumer.

## Marquee bulb and socket (#241)

`marquee-bulb.glb` — original generic Halcyon fixture, authored with scripted
Blender meshes; no external mesh or texture sources. Editable source and script:
`tools/models/marquee-bulb.blend`, `tools/models/marquee-bulb.py`.
Dimensions, material roles and runtime contract: `docs/marquee-bulb-model.md`.

## Window and portrait poster frames (#240)

`poster-frame-window.glb` and `poster-frame-wall.glb` are original generic Halcyon
geometry under this repository's license. No downloaded meshes, branded artwork
or reference photographs are incorporated. Blender 5.2 source and reproducible
script: `tools/models/poster-frames.blend` and `tools/models/poster-frames.py`.
The existing scene supplies dimensions; this is not a measured period replica.
See `docs/poster-frames-model.md` for axes, UVs, named finish roles, resource cost
and integration/lifecycle evidence.

## Counter coming-soon letterboard (#239)

`coming-soon-letterboard.glb` is original generic scripted Blender mesh geometry,
under the repository license. No downloaded mesh, external texture or branded
art is included. `tools/models/coming-soon-letterboard.blend` and `.py` provide
editable source and reproduction. The envelope follows existing scene dimensions;
the original owner photo is unavailable, and hidden construction is inferred,
not a claim of manufacturer or period fidelity. See
`docs/coming-soon-letterboard-model.md` for dimensions, UVs, finishes and evidence.

## Alcove bead curtain (#232)

`alcove-curtain.glb` — original generic model authored for Halcyon using Blender
scripted mesh construction; no third-party assets or branded reference derivatives.
Editable source and generator: `tools/models/alcove-curtain.blend` and `.py`.
Dimensions, material roles, shared doorway attachment boundary and evidence:
`docs/alcove-curtain-model.md`. Distributed under the project license.

## Entrance walk-off mat (`entrance-mat.glb`)

Original generic scripted Blender mesh and procedural pile bakes for #222; no imported geometry, photographs or branded artwork. Editable source and generator: `tools/models/entrance-mat.blend` and `tools/models/entrance-mat.py`. Dimensions, material roles, UVs, resource costs and integration: [`docs/entrance-mat-model.md`](../../docs/entrance-mat-model.md). Design dimensions follow the existing scene, not a measured historical product.

## Service door and panic hardware (#223)

`service-door.glb` is original generic scripted Blender geometry under the
repository license. No external mesh, texture, or branded art was used.
Editable source and generator: `tools/models/service-door.blend` and `.py`.
Construction dimensions are estimates fitted to the existing solid-wall doorway;
this is not a manufacturer replica. See `docs/service-door-model.md` for the
paired mounting-plane adaptation, units, UVs, material roles, and delivery costs.

## Screening-room coffee table (#215)

Original scripted Blender geometry, replacing the downloaded Kenney table.
Editable source and generator: `tools/models/coffee-table.blend` and `.py`.
Wood grain derives from the existing CC0 ambientCG WoodFloor043 maps; see
`public/textures/surfaces/table-wood/NOTES.md`. Other finish maps are original.
Dimensions, construction, material roles and verification: [model notes](../../docs/coffee-table-model.md).

## Independent-shop counter television (#210)

`counter-tv.glb` is original generic scripted Blender geometry with original
seeded normal/roughness textures, distributed under the repository license.
No external mesh or branded artwork is included. Editable source and authoring:
`tools/models/counter-tv.blend` and `.py`. Dimensions follow the existing store
contract, not a measured historical product. See [model notes](../../docs/counter-tv-model.md).

## Counter-wall apparel (#205)

`counter-apparel.glb` is original generic scripted Blender geometry with original
procedural yarn and micrograin textures, under the repository license. No external
mesh, photograph or branded artwork is included. Historical garment specifics
are unverified; dimensions and mounting hardware are design estimates. Editable
source and generator: `tools/models/counter-apparel.blend` and `.py`. See
[model notes](../../docs/counter-apparel-model.md).

## Late-era checkout flat panel (#189)

`flat-panel-terminal.glb` is original generic scripted Blender geometry with
original seeded grain-normal and roughness textures under the repository license.
No source-photo pixels, logos, or downloaded geometry are included. The mixed
CRT/LCD workstation reference was inspected; dimensions and concealed construction
remain design estimates. Editable source and generator:
`tools/models/flat-panel-terminal.blend` and `.py`. See
[model notes](../../docs/flat-panel-terminal-model.md) for material, attachment,
keyboard reuse and evidence contracts.

## Tape-cleaner retail carton

`cleaner-carton.glb` is original generic scripted Blender paperboard geometry
for #181. Source, packed procedural fibre textures and generator are in
`tools/models/cleaner-carton.*` and `cleaner-carton-*.png`; no third-party artwork
or geometry. Dimensions, material roles, UVs, provenance and integration contract:
`docs/cleaner-carton-model.md`.
